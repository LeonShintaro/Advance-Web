<section class="navbar navbar-dark bg-dark navbar-expand-lg stickt-top">
    <a href ="index.php" class="navbar-brand">Shop</a>
    <button class="navbar-toggle" data-target="#main-nav" data-toggle="collapse">
        <span class="navbar-toggler-icon"></span>
    </button>
    <form class="form-inline" method="get" action="search.php">
        <input type="search" name="query" placeholder="Search our catalogue" class="form-control" value="">
        <button type="submit" class="btn btn-primary">Search</button>
    </form>
    <div class="collapse navbar-collapse" id="main-nav">
        <nav class="nav navbar-nav ml-auto">
            {% for item in navigation %}
            <a class="nav-link" href="{{item.link}}">{{item.link}} </a>
            {% endfor %}
        </nav>
    </div>
</section>